package net.apptronic.core.demoapp.core.data

import android.graphics.Color

enum class RobotColor(val hexColor: Int, val colorName: String) {
    Red(Color.RED, "Red"),
    Green(Color.GREEN, "Green"),
    Blue(Color.BLUE, "Blue"),
}